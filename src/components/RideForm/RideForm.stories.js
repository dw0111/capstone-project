import React from 'react'

import RideForm from './RideForm'

// eslint-disable-next-line import/no-anonymous-default-export
export default {
  title: 'RideForm',
  component: RideForm,
}

const Template = () => <RideForm />

export const RideFormStory = Template.bind({})
